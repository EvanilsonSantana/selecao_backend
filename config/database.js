const { database } = require("pg/lib/defaults");

module.exports = {
    dialect: 'postgres',
    host: 'localhost',
    port: 5432,
    database: 'Projeto',
    username: 'postgres',
    password: 'Evan@8674'
}